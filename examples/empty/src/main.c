#include <stdint.h>
#include <stdio.h>

#include <common.h>
#include <mesh.h>

void main(void)
{
    init_mesh();
}

void mesh_receive(uint8_t *data, size_t len, uint8_t *src, bool broadcast)
{
}
